# Comma-separated front-end origins allowed to call the API.
# Use "*" ONLY for local testing — set your real domain in production.
ALLOWED_ORIGINS=https://YOUR-FRONTEND-DOMAIN

# Max /evaluate requests per IP per minute (abuse / cost protection)
RATE_LIMIT_PER_MIN=20

# Port — most hosts set this for you automatically
PORT=8000
